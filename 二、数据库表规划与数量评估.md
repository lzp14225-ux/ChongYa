# 二、数据库表规划与数量评估

本文档用于评估瑞利杰供应链协同平台至少需要多少张数据库表，并按业务模块整理表结构规划。

结论先行：

```
Demo 最小闭环：约 38 张表
生产一期最低：约 62 张表
完整三端平台：约 80～95 张表
```

如果目标是“可维护、可上线、可审计、可追溯”的生产级系统，不建议低于 62 张表。低于这个数量，通常会牺牲三端数据权限、状态流转、库存流水、数量一致性校验、审计日志或对账追溯。

------

# 一、为什么不是十几张表就够

这个项目不是普通 CRUD 系统，而是三端协同业务系统：

```
瑞利杰内部端
供应商端
仓库端
```

核心链路是：

```
请购
  → 询价
  → 报价
  → 价格审批
  → 采购订单
  → 供应商确认
  → 送货
  → 仓库签收
  → 质检
  → 入库
  → 退货
  → 对账
```

这里至少涉及：

```
1. 用户、角色、权限、数据范围
2. 供应商、物料、仓库、客户等主数据
3. 单据主表和明细表
4. 单据状态流转记录
5. 数量变更记录
6. 库存余额和库存流水
7. 附件、通知、审计日志
8. 对账和退货追溯
```

所以表不能只按页面来建，必须按业务对象、明细、流水、日志、权限、库存和对账来拆。

------

# 二、表数量分级建议

## 2.1 Demo 最小闭环：约 38 张表

适合只验证业务流程，不适合正式上线。

特点：

```
1. 权限模型简化
2. 只保留核心主数据
3. 单据流程可跑通
4. 库存只做基础流水
5. 审计和数据权限较弱
6. Agent、Skills、OpenAPI 契约版本不单独落表
```

风险：

```
1. 三端隔离不够严谨
2. 供应商和仓库数据范围容易越权
3. 后期补审计和库存追溯成本高
4. 对账、退货、分切扩展困难
```

不建议作为正式项目目标。

## 2.2 生产一期最低：约 62 张表

这是建议的第一阶段上线目标。

特点：

```
1. 三端权限隔离完整
2. 请购到入库、退货、对账主链路完整
3. 有状态变更日志
4. 有审计日志
5. 有库存余额和库存流水
6. 有数量变更日志
7. 支持附件、通知、字典配置
8. 支持后续扩展 Agent、Skills、外部系统集成
```

推荐先按 62 张左右设计，后续再扩展到 80 张以上。

## 2.3 完整三端平台：约 80～95 张表

适合覆盖完整采购协同、外库、委外、分切、供应商绩效、复杂审批、Agent 执行记录、Skills 调用记录、外部系统集成日志。

完整版本会额外增加：

```
1. 委外加工相关表
2. 分切 BOM 和分切结果表
3. 供应商绩效评分表
4. 多级审批流表
5. Agent 执行记录表
6. Skill 调用记录表
7. 外部系统同步记录表
8. Webhook 事件表
9. 报表快照表
10. 库存快照表
```

------

# 三、生产一期最低表规划：62 张

下面是建议的生产一期最低表清单。

## 3.1 基础组织、用户、权限：13 张

| 序号 | 表名 | 说明 |
|---:|---|---|
| 1 | users | 用户表 |
| 2 | roles | 角色表 |
| 3 | permissions | 权限点表 |
| 4 | user_roles | 用户角色关联表 |
| 5 | role_permissions | 角色权限关联表 |
| 6 | organizations | 组织表 |
| 7 | departments | 部门表 |
| 8 | user_organizations | 用户组织关联表 |
| 9 | menus | 菜单表 |
| 10 | role_menus | 角色菜单关联表 |
| 11 | data_scope_rules | 数据范围规则表 |
| 12 | login_sessions | 登录会话表 |
| 13 | operation_idempotency_keys | 幂等键表 |

说明：

```
供应商端和仓库端的数据隔离不能只靠前端菜单控制，必须有数据范围规则和后端权限校验。
```

## 3.2 主数据：10 张

| 序号 | 表名 | 说明 |
|---:|---|---|
| 14 | suppliers | 供应商主表 |
| 15 | supplier_users | 供应商用户关联表 |
| 16 | materials | 物料主表 |
| 17 | material_categories | 物料分类表 |
| 18 | warehouses | 仓库主表 |
| 19 | warehouse_users | 仓库用户关联表 |
| 20 | customers | 客户主表 |
| 21 | external_warehouses | 外库表 |
| 22 | dictionaries | 字典类型表 |
| 23 | dictionary_items | 字典明细表 |

说明：

```
物料、供应商、仓库、客户是采购协同的基础主数据。字典表用于维护状态、退货原因、异常原因、送货方式、质检结果等配置。
```

## 3.3 请购、询价、报价、采购订单：13 张

| 序号 | 表名 | 说明 |
|---:|---|---|
| 24 | requisitions | 请购单主表 |
| 25 | requisition_items | 请购单明细表 |
| 26 | inquiries | 询价单主表 |
| 27 | inquiry_items | 询价单明细表 |
| 28 | quotation_requests | 供应商报价邀请表 |
| 29 | quotations | 报价单主表 |
| 30 | quotation_items | 报价单明细表 |
| 31 | price_approvals | 价格审批主表 |
| 32 | price_approval_items | 价格审批明细表 |
| 33 | purchase_orders | 采购订单主表 |
| 34 | purchase_order_items | 采购订单明细表 |
| 35 | purchase_order_confirmations | 供应商订单确认表 |
| 36 | purchase_order_change_logs | 采购订单变更记录表 |

说明：

```
请购、询价、报价、采购订单都建议主表和明细表分开。报价邀请表用于支持一个询价单发给多个供应商，并隔离供应商报价可见范围。
```

## 3.4 送货、签收、质检、入库：12 张

| 序号 | 表名 | 说明 |
|---:|---|---|
| 37 | delivery_orders | 送货执行单主表 |
| 38 | delivery_order_items | 送货执行单明细表 |
| 39 | delivery_packages | 送货包装/箱号表 |
| 40 | receipt_orders | 签收单主表 |
| 41 | receipt_order_items | 签收单明细表 |
| 42 | quality_inspections | 质检单主表 |
| 43 | quality_inspection_items | 质检单明细表 |
| 44 | inbound_orders | 入库单主表 |
| 45 | inbound_order_items | 入库单明细表 |
| 46 | barcodes | 条码表 |
| 47 | logistics_records | 物流记录表 |
| 48 | receiving_tasks | 仓库待收货任务表 |

说明：

```
送货、签收、质检、入库必须拆开。供应商送货不等于仓库签收，仓库签收不等于质检合格，质检合格也不等于已经入库。
```

## 3.5 库存、流水、数量一致性：7 张

| 序号 | 表名 | 说明 |
|---:|---|---|
| 49 | inventory_balances | 当前库存余额表 |
| 50 | inventory_ledgers | 库存流水表 |
| 51 | inventory_lots | 批次库存表 |
| 52 | inventory_reservations | 库存占用表 |
| 53 | document_ledgers | 单据流水表 |
| 54 | state_change_logs | 状态变更日志表 |
| 55 | quantity_change_logs | 数量变更日志表 |

说明：

```
库存余额可以重算，库存流水不能随意修改。所有库存变化必须先写库存流水，再汇总更新库存余额。
```

## 3.6 异常、退货、对账：7 张

| 序号 | 表名 | 说明 |
|---:|---|---|
| 56 | exception_orders | 异常单主表 |
| 57 | exception_order_items | 异常单明细表 |
| 58 | return_orders | 退货单主表 |
| 59 | return_order_items | 退货单明细表 |
| 60 | reconciliations | 对账单主表 |
| 61 | reconciliation_items | 对账单明细表 |
| 62 | reconciliation_adjustments | 对账调整表 |

说明：

```
退货和对账不能只挂在采购订单上，必须能追溯到送货单、签收单、质检单、入库单和库存流水。
```

------

# 四、生产一期建议再补充的表：10～15 张

如果周期允许，建议在生产一期直接补到 72～77 张表，后期会更稳。

| 表名 | 说明 |
|---|---|
| attachments | 附件表 |
| attachment_links | 附件业务关联表 |
| notifications | 通知表 |
| notification_receivers | 通知接收人表 |
| audit_logs | 审计日志表 |
| approval_flows | 审批流定义表 |
| approval_nodes | 审批节点定义表 |
| approval_tasks | 审批任务表 |
| comments | 业务评论/备注表 |
| tags | 业务标签表 |
| import_tasks | 导入任务表 |
| export_tasks | 导出任务表 |
| openapi_contract_versions | OpenAPI 契约版本表 |
| file_upload_sessions | 文件上传会话表 |
| system_configs | 系统配置表 |

建议优先补：

```
attachments
attachment_links
notifications
audit_logs
approval_tasks
import_tasks
export_tasks
```

这些表对生产运维、问题追溯和用户协同很关键。

------

# 五、完整版本扩展表：约 20 张以上

完整版本可以在二期或三期扩展。

## 5.1 分切和委外

| 表名 | 说明 |
|---|---|
| temporary_boms | 临时 BOM 主表 |
| temporary_bom_items | 临时 BOM 明细表 |
| cutting_tasks | 分切任务表 |
| cutting_task_items | 分切任务明细表 |
| cutting_results | 分切结果表 |
| cutting_result_items | 分切结果明细表 |
| outsourcing_orders | 委外单主表 |
| outsourcing_order_items | 委外单明细表 |

## 5.2 供应商绩效

| 表名 | 说明 |
|---|---|
| supplier_score_rules | 供应商评分规则表 |
| supplier_score_snapshots | 供应商评分快照表 |
| supplier_delivery_metrics | 供应商交付指标表 |
| supplier_quality_metrics | 供应商质量指标表 |

## 5.3 Agent、Skills、工具执行记录

| 表名 | 说明 |
|---|---|
| agent_runs | Agent 执行记录表 |
| agent_run_steps | Agent 执行步骤表 |
| skill_runs | Skill 调用记录表 |
| tool_runs | 工具执行记录表 |

## 5.4 外部系统集成

| 表名 | 说明 |
|---|---|
| integration_apps | 外部系统应用配置表 |
| integration_sync_logs | 外部系统同步日志表 |
| webhook_events | Webhook 事件表 |
| webhook_deliveries | Webhook 投递记录表 |

------

# 六、关键表设计原则

## 6.1 所有业务单据都建议主表 + 明细表

例如：

```
purchase_orders
purchase_order_items

delivery_orders
delivery_order_items

return_orders
return_order_items
```

原因：

```
1. 一个单据通常包含多个物料
2. 不同明细可能有不同数量、批次、仓库、状态
3. 后续退货、对账、库存流水都要关联到明细级别
```

## 6.2 状态不要只存在业务主表

业务主表可以有当前状态字段，但必须有独立状态变更日志：

```
state_change_logs
```

状态日志至少记录：

```
business_type
business_id
from_status
to_status
action
operator_id
operator_type
source_app
changed_at
reason
```

## 6.3 数量变更要单独留痕

核心数量变化必须记录到：

```
quantity_change_logs
```

至少记录：

```
business_type
business_id
business_item_id
quantity_type
before_quantity
change_quantity
after_quantity
source_document_type
source_document_id
operator_id
created_at
```

## 6.4 库存必须用流水驱动余额

不能直接修改当前库存余额。

正确方式：

```
业务动作
  → 写 inventory_ledgers
  → 汇总更新 inventory_balances
```

这样库存异常时可以通过流水重算。

## 6.5 供应商和仓库的数据权限要落库

不能只在代码里硬编码。

建议：

```
data_scope_rules
supplier_users
warehouse_users
user_organizations
```

这几张表共同决定用户能看哪些数据。

------

# 七、最终建议

本项目数据库表数量建议这样控制：

```
不要低于 62 张表
一期最好控制在 70 张左右
完整版本允许增长到 90 张左右
```

第一阶段不要追求表少，而要保证下面几条链路清晰：

```
1. 用户 → 角色 → 权限 → 数据范围
2. 请购 → 询价 → 报价 → 采购订单
3. 采购订单 → 送货 → 签收 → 质检 → 入库
4. 入库/签收 → 退货 → 对账
5. 单据状态 → 状态日志 → 审计日志
6. 数量变化 → 数量日志 → 库存流水 → 库存余额
```

只要这些链路清楚，后续扩展 Agent、Skills、分切、委外、ERP/WMS 集成都不会推翻底层数据库设计。
