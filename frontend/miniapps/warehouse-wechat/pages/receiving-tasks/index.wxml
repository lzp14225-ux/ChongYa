<view class="page">
  <view class="card" wx:for="{{tasks}}" wx:key="code">
    <view class="card-title">{{item.code}}</view>
    <view class="muted">{{item.desc}}</view>
    <view class="tag">{{item.status}}</view>
    <button class="button" bindtap="receive">确认签收</button>
  </view>
</view>
